import Head from "next/head";
import { App } from "../components/App";
import { NoSSRWrapper } from "../components/lib/NoSSRWrapper";

const Home = () => {
  return (
    <>
      <Head>
        <title>Chords exercise</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/guitar.png" />
      </Head>
      <main className="flex h-screen flex-col items-center justify-center bg-gradient-to-b from-red-900 to-black">
        <NoSSRWrapper>
          <App />
        </NoSSRWrapper>
      </main>
    </>
  );
};

export default Home;
